import React from "react";
import "../styles/Project.css";

interface Props {
  title: string;
  shortDesc: string;
  tags: string;
  image: string;
}
export default function Project({ title, shortDesc, tags, image }: Props) {
  return <div id="top-container">asdasd</div>;
}
